# ui

Shared React component library for `apps/admin-web`. Not used by the macOS app.

## Conventions

- Headless primitives + styled wrappers.
- Tailwind tokens (TBD).
- WCAG 2.1 AA accessibility baseline.
- Storybook for visual review (optional in v1).
