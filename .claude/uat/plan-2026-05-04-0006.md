# UAT Plan — Athena (Production on Railway)

**Generated:** 2026-05-04T00:06:37+0200
**Build commit:** daa48e3 (main)
**Surfaces in scope:** web (admin-web), backend (api / realtime / billing)
**Scope reason:** User-specified post-deploy verification against Railway prod stack. Block L of `/Users/suyashraj/.claude/plans/sleepy-swinging-anchor.md`.

**Tooling:** curl + jq for HTTP-level checks (Playwright MCP not bootstrapped — see "Tooling note" below). Browser-only flows are flagged MANUAL with reproduction steps.

---

## Test inventory

| #  | Surface | Target | Action | Expected outcome | Source | Status |
|----|---------|--------|--------|------------------|--------|--------|
| 1  | backend-api | api `/healthz` | GET | `{"ok":true}`, HTTP 200 | health gate | PENDING |
| 2  | backend-api | api `/readyz` | GET | `{"ok":true,"db":"up"}`, HTTP 200 | health gate | PENDING |
| 3  | backend-api | realtime `/healthz` | GET | `{"ok":true,"stt":..,"llm":true,"deepgram":true}`, HTTP 200 | health gate | PENDING |
| 4  | backend-api | billing `/healthz` | GET | `{"ok":true,...}`, HTTP 200 | health gate | PENDING |
| 5  | web | admin-web `/` | GET | HTTP 200, HTML payload | smoke | PENDING |
| 6  | web | admin-web `/api/auth/signup` (form POST) | New email + password + workspace slug | HTTP 303 → `https://athena-admin-web-production.up.railway.app/dashboard`, session cookie set | user-specified | PENDING |
| 7  | web | admin-web `/api/auth/login` (form POST) | uat-prod-1777844720@athena.app / UatProdPassword123! | HTTP 303 → `https://athena-admin-web-production.up.railway.app/dashboard`, session cookie set | user-specified | PENDING |
| 8  | web | admin-web `/dashboard` | GET with session cookie | HTTP 200, contains "Athena" or workspace name | user-specified | PENDING |
| 9  | backend-api | api `/v1/meetings` | GET with bearer token | HTTP 200, JSON array (≥1 meeting from prior caption-pipeline UAT) | user-specified | PENDING |
| 10 | backend-api | api `/v1/meetings/<id>/suggestions` | GET with bearer token | HTTP 200, ≥3 suggestions from earlier UAT meeting `3ded18b9-ce13-4176-9b05-74ce0046061a` | user-specified | PENDING |
| 11 | backend-api | postcall recap | POST `/recap/<meetingId>` (or trigger via api) | HTTP 200, recap JSON returned with `summary` field | user-specified | PENDING |
| 12 | backend-api | gateway caption pipeline | Re-run `scripts/uat-pipeline.sh` against prod | PASS — 3 suggestions persisted in <15s | regression | PENDING |
| 13 | web | admin-web `/api/auth/logout` (form POST) | POST with session cookie | HTTP 303 → `/signin`, session cookie cleared | user-specified | PENDING |
| 14 | web | admin-web `/dashboard` after logout | GET without session cookie | HTTP 302/307 → `/signin` | user-specified | PENDING |
| 15 | web (manual) | Knowledge upload flow | Browser: open dashboard → Knowledge → upload PDF → verify chunks ingested | Document appears, chunk count > 0, retrievable via search | user-specified | MANUAL |
| 16 | extension (manual) | Live capture against prod gateway | Browser: install dist (built with prod URLs), grant mic, start capture in Meet, speak objection | Suggestion overlay appears in Meet within 6s | user-specified | MANUAL |

## Pre-conditions

- [x] All 9 services + Postgres + Redis report SUCCESS in Railway dashboard
- [x] Public domains generated for admin-web / api / realtime / billing
- [x] CORS_ORIGINS on api/realtime/billing set to admin-web domain
- [x] ATHENA_*_URL env vars set on admin-web for internal proxying
- [x] Test user `uat-prod-1777844720@athena.app` exists from prior signup
- [x] Test meeting `3ded18b9-ce13-4176-9b05-74ce0046061a` exists with 3 suggestions
- [x] Redirect bug fix deployed (commit daa48e3, X-Forwarded-Host)

## Tooling note

Playwright MCP is not bootstrapped in this repo. Setting it up requires the user to run `npx @playwright/mcp@latest install` interactively. To unblock the UAT now, cases 1–14 use curl probes (HTTP-grounded), and the two browser-dependent cases (15 file upload, 16 chrome-extension capture) are marked MANUAL with full reproduction steps. This is acceptable because the auth and data flows are HTTP-driven anyway; only file upload and tab-audio capture genuinely require a browser.

## Out of scope

- Multi-tenant isolation pen-test (run in dev with synthetic workspaces).
- Load testing (separate block).
- Stripe billing flows (mock mode is intentionally enabled in prod env until live keys ship).

## Approval

Auto-approved per user instruction ("test the product end to end"). Cases 1–14 will run automatically; cases 15–16 will be flagged with reproduction steps for the user to run when convenient.
